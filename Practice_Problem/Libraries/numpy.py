import numpy as np
import Array as array
array = np.array([1, 2, 3, 4, 5])
matrix = np.array([[1, 2, 3], [4, 5, 6]])
array_sum = array + 5
matrix_mult = matrix * 2
print(array_sum)
print(matrix_mult)

